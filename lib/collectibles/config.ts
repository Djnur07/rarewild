/** Collectible tuning (world px, ms). */
export const COLLECTIBLE_RADIUS = 14;
/** Rara only needs to be this close to the edge of an item to collect it: a little forgiveness. */
export const PICKUP_FORGIVENESS = 4;
/** Idle bobbing of the placeholder art (rendering only). */
export const BOB_AMPLITUDE = 3;
export const BOB_PERIOD_MS = 1500;
